<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

    <!-- <style name="Theme.App.Starting" parent="Theme.SplashScreen"> -->
        <!-- Заливка фона -->
        <!-- <item name="windowSplashScreenBackground">@color/bgColor</item>  -->
     
        <!-- Замены значка в центре начального окна. -->
        <!-- <item name="windowSplashScreenAnimatedIcon">@drawable/launch_background</item> -->

        <!-- Указания продолжительности анимации значка заставки.
            Если объект можно анимировать и прорисовывать с помощью AnimationDrawable (только для andoid 12 )-->
        <!-- <item name="windowSplashScreenAnimationDuration">1000</item> -->
     
        <!-- Установка фона за значком заставки (если недостаточно контраста между фоном окна и значком) -->
        <!-- <item name="android:windowSplashScreenIconBackgroundColor">@color/...</item> -->

        <!-- Отображения изображения в нижней части заставки -->
        <!-- <item name="android:windowSplashScreenBrandingImage">@drawable/...</item> -->

        <!-- Если всегда хотите, чтобы отображался анимированный значок, установите для этого значение icon_preferred. -->
        <!-- <item name="android:windowSplashScreenBehavior">icon_preferred</item> -->

        <!-- Set the theme of the Activity that directly follows your splash screen. -->
        <!-- Required -->
        <!-- <item name="postSplashScreenTheme">@style/Theme.App</item>
     </style> -->
     
</resources>
